import { LoadingState } from '@/components/ui/LoadingState'

export default function Loading() {
  return <LoadingState title="Cargando proyectos..." description="Estamos reuniendo tus proyectos y avances." />
}
