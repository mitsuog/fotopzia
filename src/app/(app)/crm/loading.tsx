import { LoadingState } from '@/components/ui/LoadingState'

export default function Loading() {
  return <LoadingState title="Cargando CRM..." description="Estamos preparando tus contactos y oportunidades." />
}
