import { LoadingState } from '@/components/ui/LoadingState'

export default function Loading() {
  return <LoadingState title="Cargando contratos..." description="Estamos preparando los contratos y su estado de firma." />
}
